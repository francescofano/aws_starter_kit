# Stack Configuration
STACK_NAME=MyWebsiteStack

# Domain Configuration
DOMAIN_NAME=example.com
HOSTED_ZONE_ID=ZXXXXXXXXXXXX
SKIP_DOMAIN_SETUP=true  # Set to true to skip domain setup and use only CloudFront URL

# Frontend Configuration
# Path is relative to the backend directory
FRONTEND_PATH=../frontend/dist

# AWS Configuration (optional, can use AWS CLI profile)
# AWS_REGION=us-east-1
# AWS_PROFILE=default 